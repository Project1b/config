server:
  port: 8081

restClient:
  accountUrl: http://gateway-server-service/api/account

eureka:
  client:
    service:
      url:
        defaultZone: http://localhost:8096/eureka
  instance:
    preferIpAddress: true
    
management:
  endpoints:
    web:
      exposure:
        incude: "*"

